<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:name=".ScApplication"
        android:allowBackup="false"
        android:extractNativeLibs="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".pages.AdvanceSettingPageActivity" />
        <activity android:name=".pages.FeedbackPageActivity" />
        <activity android:name=".pages.FunctionTestPageActivity" />
        <activity android:name=".pages.NotificationLogPageActivity" />
        <activity android:name=".pages.AttentionStatsActivity" />
        <activity android:name=".pages.BlockChainPageActivity" />
        <activity android:name=".pages.CheckUpdatePageActivity" />
        <activity android:name=".pages.AboutThisPageActivity" />
        <activity android:name=".pages.FilterListActivity" />
        <activity android:name=".pages.AppInfoPageActivity" />
        <activity android:name=".pages.SleepTimeSettingActivity" />
        <activity android:name=".pages.WakeUptimeSettingActivity" />
        <activity android:name=".pages.ReminderSettingActivity" />

        <!--
            Ends a precise screen-on window on devices where no lock permission
            is granted. Its own task, so it never lands on top of the app's UI,
            and out of recents because the user is not meant to see it at all.
        -->
        <activity
            android:name=".pages.NightGlowActivity"
            android:theme="@style/ScreenOffTheme"
            android:taskAffinity=".ScreenOff"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:launchMode="singleInstance"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:exported="false" />
        <activity
            android:name=".pages.ScreenOffActivity"
            android:theme="@style/ScreenOffTheme"
            android:taskAffinity=".ScreenOff"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:launchMode="singleInstance"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:exported="false" />
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ScBaseActivity" />

        <service
            android:name=".services.ScNotificationListenerService"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!--
            Optional: only bound after the user enables it, and only used to
            perform GLOBAL_ACTION_LOCK_SCREEN at the end of a precise screen-on
            window. See ScLockScreenAccessibilityService.
        -->
        <service
            android:name=".services.ScLockScreenAccessibilityService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:label="@string/accessibility_service_label"
            android:exported="true">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/lock_screen_accessibility_service" />
        </service>

        <receiver android:name=".receiver.PackageReplacedReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- Only ever triggered by this app's own repeat-reminder alarm. -->
        <receiver android:name=".receiver.ReminderAlarmReceiver"
            android:exported="false" />

        <!-- Only ever triggered by this app's own function-test alarm. -->
        <receiver android:name=".receiver.FunctionTestAlarmReceiver"
            android:exported="false" />

    </application>

</manifest>